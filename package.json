{
  "name": "catan-open",
  "version": "0.1.0",
  "private": true,
  "description": "Catán multijugador hecho con React + Phaser + Tailwind (cliente) y Colyseus (servidor)",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n shared,server,client -c blue,magenta,cyan \"npm:dev:shared\" \"npm:dev:server\" \"npm:dev:client\"",
    "dev:shared": "npm -w shared run dev",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w shared run build && npm -w server run build && npm -w client run build",
    "typecheck": "npm -w shared run typecheck && npm -w server run typecheck && npm -w client run typecheck",
    "start": "npm -w server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
