# URL del servidor Colyseus (Railway en producción).
# En desarrollo se usa el valor por defecto ws://localhost:2567.
# Esta variable se inyecta en build time (Vite) desde GitHub Secrets.
VITE_SERVER_URL=wss://tu-servidor.up.railway.app
